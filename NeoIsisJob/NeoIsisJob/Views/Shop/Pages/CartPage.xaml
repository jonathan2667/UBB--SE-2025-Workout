<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WorkoutApp.View.Pages.CartPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WorkoutApp.View.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:component="using:WorkoutApp.View.Components"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel Orientation="Vertical" Height="610">
        <TextBlock
            Text="Shopping Cart"
            FontSize="24"
            FontWeight="Bold"
            HorizontalAlignment="Left"
            Margin="20,20,0,10"/>
        <StackPanel Orientation="Horizontal">
            <component:VerticalRemovableProductListComponent
                x:Name="ProductListViewControl"
                CartItemClicked="VerticalProductListControl_ProductClicked"
                CartItemRemoved="VerticalProductListControl_CartItemRemoved"
                Margin="20,0,0,0"
                Width="850"
                Height="Auto"/>
            <StackPanel Orientation="Vertical" Margin="20,0,0,0">
                <TextBlock
                    Text="Total:"
                    FontSize="18"
                    FontWeight="Bold"
                    HorizontalAlignment="Left"/>
                <TextBlock
                    x:Name="TotalPriceTextBlock"
                    FontSize="18"
                    HorizontalAlignment="Left"/>
                <Button
                    x:Name="CheckoutButton"
                    Content="Checkout"
                    Click="CheckoutButton_Click"
                    Width="100"
                    Height="30"
                    Margin="0,20,0,0"/>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</Page>
